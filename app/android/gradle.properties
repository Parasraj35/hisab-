org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# Pinned to JDK 21 LTS: the system JDK (26) and Android Studio's JBR (25) are
# both too new for the Kotlin compiler Gradle bundles for .gradle.kts scripts —
# it fails to parse the version string ("IllegalArgumentException: 26.0.2")
# before any app code is read.
org.gradle.java.home=C\:\\Program Files\\Eclipse Adoptium\\jdk-21.0.12.101-hotspot
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin's incremental compiler chokes on the project (D:) and Pub Cache (C:)
# living on different drive letters — "Could not close incremental caches".
# Disabling incremental compilation avoids it at a small clean-build cost.
kotlin.incremental=false
